<%= form_for @item, :html => { :multipart => true } do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
		<% @item.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
		<%= f.hidden_field :room_id, :value => current_room.id %>
		<%= f.hidden_field :status, :value => 'active' %>
    </div>
    <div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
    </div>
    <div class="field">
		<%= f.label :price %><br />
		$<%= f.text_field :price %>
    </div>
    <div class="field">
		<%= f.label :description %><br />
		<%= f.text_area :description %>
    </div>

<!-- PHOTOS CURRENTLY NOT BEING ADDED OR REMOVED -->
  <!--<div class="field">
	<%= f.label :photo %><br />
	<%= f.file_field :photo %>
	<% if @item.photo.exists? then %>
		<p>Current Photo</>
		<%= image_tag @item.photo.url(:thumb) %>
		<%= link_to 'Remove Photo', removephoto_path(@item), method: :delete, :confirm => 'Are you sure you want to remove this photo?' %>
	<% end %>
  </div>-->
<!-- ########################################### -->

    <div class="actions">
		<%= f.submit %>
    </div>
<% end %>
